import React, { VFC } from "react";
import SearchInputBox from "./SearchInputBox";

const Navbar: VFC = () => {
  return (
    <div>
      Navbar
      <SearchInputBox />
    </div>
  );
};

export default Navbar;
